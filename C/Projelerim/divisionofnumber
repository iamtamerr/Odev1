#include <stdio.h>
#include <stdlib.h>


int main() {
	
	int number,i,result;
	
	printf("Enter a number: ");
	scanf("%d",&number);
	
	printf("Division of %d are: \n",number);
	
	for(i=1;i<=number;i++)
	{
		if(number % i == 0)
			printf("%d ",i);	
	}
	
	
	return 0;
}
